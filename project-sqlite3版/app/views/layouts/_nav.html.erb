<nav class="navbar navbar-default" role="navigation">
  <div class="container-fluid ">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header ">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="/">Shopping Mall</a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li class="<%= "active" if params[:controller] == "products" %>"><%= link_to "Product", products_path %>
        </li>
        <li class="<%= "active" if params[:controller] == "orders" %>"><%= link_to "Cart", orders_path %>
        </li>
         <li class="<%= "active" if params[:controller] == "shops" %>"><%= link_to "Shop", shops_path %>
        </li>
      </ul>

  		<!-- Devise actions -->
  		<ul class="nav navbar-nav navbar-right">
        <li>
          <%= form_tag("/products", method: "get", class: "navbar-form") do %>
            <div class="form-group">
              <%= text_field_tag(:q, nil, class: "form-control", placeholder: "Search") %>
            </div>
              <%= submit_tag("Search", class: "btn btn-default") %>
          <% end %>
        </li>

        <li class="<%= "active" if params[:controller] == "users" %>">
            <%= link_to "User Info",userinfos_path %>
        </li>

  			<% if user_signed_in? %>
  			  <li>
  			  <%= link_to('Logout', destroy_user_session_path, :method => :delete) %>
  			  </li>
  			<% else %>
  			  <li>
  			  <%= link_to('Login', new_user_session_path)  %>
  			  </li>
  			<% end %>
  		</ul>

    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>
