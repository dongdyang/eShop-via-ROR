<div class="row">
  <hr>
  <p>
      <%= link_to ('<span class="glyphicon glyphicon-remove"></span>').html_safe, shop_product_path(@shop, product), method: :delete, data: { confirm: 'Are you sure?' } %>
      <%#= link_to 'Destroy', product_comment_path(@product, comment), method: :delete, data: { confirm: 'Are you sure?' } %>
  </p>
  <p><%= product.name %></p>
</div>
